apiVersion: v1
kind: Service
metadata:
  name: finantrack-frontend-service
spec:
  selector:
    app: finantrack-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
